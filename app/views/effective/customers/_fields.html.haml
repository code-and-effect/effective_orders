.effective-orders-customer{ data: { stripe: stripe.to_json, submit: submit.to_json } }
  - if customer.status == 'past_due'
    %p We ran into an error processing your last payment.

  - if customer.active_card.present? && customer.token_required?
    %p
      Please update your card details to continue:
      %span.active-card

  - elsif customer.active_card.present?
    %p
      All charges will be made to your existing card:
      %span.active-card= customer.active_card

  - elsif customer.active_card.blank?
    %p
      No credit card on file. Please click below to add a card:
      %span.active-card

  %p= link_to 'Update Card Details', '#', class: 'btn btn-default btn-change-card'
