%span
  = link_to('View', (datatables_admin_path? ? effective_orders.admin_order_path(order) : effective_orders.order_path(order)))
  - unless order.purchased?
    &bull;
    = link_to 'Mark as Paid', effective_orders.mark_as_paid_admin_order_path(order), method: :post, data: { confirm: 'Are you sure you want to mark this order as paid?' }
